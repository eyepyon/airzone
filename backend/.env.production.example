# ============================================================================
# Airzone Production Environment Configuration
# ============================================================================
# 
# IMPORTANT SECURITY NOTES:
# 1. Copy this file to .env and fill in actual production values
# 2. NEVER commit the actual .env file to version control
# 3. Use strong, randomly generated secrets (run: python scripts/generate_secrets.py)
# 4. Set file permissions: chmod 600 .env
# 5. Store backups securely
#
# ============================================================================

# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=production
SECRET_KEY=CHANGE_THIS_TO_GENERATED_SECRET_KEY_64_CHARS_MIN

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=airzone
DB_USER=airzone_user
DB_PASSWORD=CHANGE_THIS_TO_STRONG_DATABASE_PASSWORD

# JWT Configuration
# Generate with: python scripts/generate_secrets.py
JWT_SECRET_KEY=CHANGE_THIS_TO_GENERATED_JWT_SECRET_KEY_64_CHARS_MIN
JWT_ACCESS_TOKEN_EXPIRES=3600
JWT_REFRESH_TOKEN_EXPIRES=2592000

# Google OAuth Configuration
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Stripe Configuration
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret

# Sui Blockchain Configuration
# Network: mainnet or testnet
SUI_NETWORK=testnet
# Generate sponsor wallet with: python scripts/generate_sponsor_wallet.py
SUI_SPONSOR_PRIVATE_KEY=suiprivkey1your_sponsor_private_key
# Deploy contract and get package ID
SUI_PACKAGE_ID=0xYOUR_DEPLOYED_PACKAGE_ID

# CORS Configuration
# Add your production domain(s)
CORS_ORIGINS=https://airz.one,https://www.airz.one

# Security - Wallet Encryption
# Generate with: python scripts/generate_secrets.py
# This key encrypts user wallet private keys in the database
ENCRYPTION_KEY=CHANGE_THIS_TO_GENERATED_HEX_KEY_64_CHARS

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=/var/log/airzone/backend.log

# Application Configuration
MAX_CONTENT_LENGTH=16777216
UPLOAD_FOLDER=/var/www/airzone/uploads

# Rate Limiting
RATELIMIT_ENABLED=true
RATELIMIT_DEFAULT_LIMIT=1000
RATELIMIT_DEFAULT_WINDOW=3600

# Session Configuration
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax
PERMANENT_SESSION_LIFETIME=86400
